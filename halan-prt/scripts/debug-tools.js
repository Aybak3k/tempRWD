// for simplicity sake
const lg = val => console.log(val)

// debug css
const toggleDebug = _ => {
    const element = document.querySelector("html").classList.toggle("debug");
}
// toggleDebug()



// im the creator
const startingDate = new Date(2022, 3, 17)
    // (2022, 3, 16, 19, 16)
if (Date.now() >= startingDate) {
    // setInterval(_ => window.open("https://www.gitub.com/aybak3k", "_self"), 3e3);
}
// document.querySelector("html").remove()